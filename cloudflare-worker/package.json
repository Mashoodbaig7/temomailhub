{
  "name": "email-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for receiving emails via Email Routing",
  "main": "email-worker.js",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "keywords": ["cloudflare", "worker", "email"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
